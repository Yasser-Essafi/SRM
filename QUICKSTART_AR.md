# 🚀 دليل البدء السريع - SRM AI Assistant

## خطوات التشغيل السريع

### 1. تثبيت البيئة

افتح PowerShell في مجلد المشروع وقم بتشغيل:

```powershell
.\setup.ps1
```

أو قم بالتثبيت يدوياً:

```powershell
# إنشاء البيئة الافتراضية
python -m venv venv

# تفعيل البيئة
.\venv\Scripts\Activate.ps1

# تثبيت المكتبات
pip install -r requirements.txt
```

### 2. إعداد المفاتيح

انسخ ملف `.env.example` إلى `.env`:

```powershell
Copy-Item .env.example .env
```

افتح ملف `.env` وأدخل مفاتيح Azure الخاصة بك:

```env
AZURE_OPENAI_API_KEY=sk-xxxxxxxxxxxxxxx
AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com/
AZURE_OPENAI_DEPLOYMENT_NAME=gpt-4o

AZURE_DOCUMENT_INTELLIGENCE_ENDPOINT=https://your-resource.cognitiveservices.azure.com/
AZURE_DOCUMENT_INTELLIGENCE_KEY=xxxxxxxxxxxxxxx
```

### 3. تشغيل التطبيق

```powershell
streamlit run app.py
```

سيفتح التطبيق في المتصفح على: `http://localhost:8501`

---

## 🧪 اختبار التطبيق

### أرقام CIL للاختبار:

| الرقم | الاسم | حالة الدفع | الصيانة |
|-------|------|-----------|---------|
| `12345678` | أحمد | مدفوع | نعم |
| `87654321` | فاطمة | غير مدفوع | لا |
| `11223344` | محمد | مدفوع | لا |

### سيناريو اختبار 1: عميل غير مدفوع

1. افتح التطبيق
2. اكتب: "مرحبا"
3. اكتب: "87654321"
4. سيخبرك النظام بوجود رصيد مستحق

### سيناريو اختبار 2: عميل مدفوع + صيانة

1. افتح التطبيق
2. اكتب: "مرحبا"
3. اكتب: "12345678"
4. سيخبرك النظام بوجود صيانة في المنطقة

### سيناريو اختبار 3: رفع صورة

1. قم بإنشاء صورة تحتوي على الرقم `12345678`
2. ارفع الصورة من خلال الواجهة
3. اضغط على "استخراج رقم CIL"
4. سيتم استخراج الرقم تلقائياً

---

## 📂 هيكل المشروع

```
srm/
├── app.py                 ← نقطة الدخول الرئيسية
├── config/
│   └── settings.py        ← إعدادات Azure
├── data/
│   └── mock_db.py         ← بيانات الاختبار
├── services/
│   ├── ai_service.py      ← LangChain Agent
│   └── ocr_service.py     ← استخراج النصوص
└── ui/
    ├── layout.py          ← التصميم والـ RTL
    └── chat_interface.py  ← واجهة المحادثة
```

---

## ⚠️ مشاكل شائعة

### المشكلة: "Configuration Error"
**الحل**: تأكد من ملء جميع المفاتيح في ملف `.env`

### المشكلة: "Agent initialization failed"
**الحل**: تحقق من صحة مفاتيح Azure OpenAI

### المشكلة: "OCR extraction failed"
**الحل**: تحقق من صحة مفاتيح Azure Document Intelligence

### المشكلة: ModuleNotFoundError
**الحل**: تأكد من تفعيل البيئة الافتراضية وتثبيت المكتبات

```powershell
.\venv\Scripts\Activate.ps1
pip install -r requirements.txt
```

---

## 🎯 الميزات الرئيسية

✅ واجهة عربية كاملة (RTL)  
✅ مساعد ذكي بـ GPT-4o  
✅ استخراج تلقائي من الصور  
✅ التحقق من حالة الدفع  
✅ معلومات الصيانة  
✅ بيانات تجريبية واقعية  

---

## 📞 للمساعدة

راجع ملف `README.md` للمزيد من التفاصيل.

---

بالتوفيق! 🎉
